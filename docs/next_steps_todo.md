# 乐跳 ATS (Letiao) 下一步开发 TODO 指南

> **当前节点总结**：
> 基础设施（阶段 0）已全部落实；全局暗色高级视觉规范及组件库（CSS）已搭建成型；业务链路（阶段 1）的起点 **“1.2 职位发布管理模块”** 及其端到端全链路已大功告成且稳定运行。

系统接下来的重心是贯穿**真正的业务流**：解决找人（候选人入库）、分类（AI扫描识别）、推人（协同看板）问题。以下是为您规划的高优 TODO 拆解清单，可直接派发给团队或指派给我依次实现。

---

## 🔥 冲刺 1：打通简历流（候选人模块基建）- *最关键*

“职位”有了，接下来就是往里“倒入简历”。这是 ATS 系统最大的核心壁垒，建议马上开始执行。

- [ ] **TODO 1: 建立候选人 API 与数据库接入 (后端)**
  - 构建 `CandidateRepository.cs` 与 `CandidateController.cs`。
  - 实现标准的新增候选人接口、分页列表查询接口。
  - **规则前置 (1.4 需求)**：在创建候选人方法中直接写入防撞单逻辑——“当手机号在 `ats_candidate` 中存在时，抛出业务拦截报错并要求合并”。
- [ ] **TODO 2: 前端上传与智能识别入口 (前端)**
  - 在 `src/views/candidates/` 新建候选人公海/私海列表页 `CandidateListView.vue`。
  - 搭配我们新出的 `glass-panel` 设计风格，用弹窗包装一个上传收集入口（调用我们已有的 `/api/upload/resume`）。
- [ ] **TODO 3: 找库外接 AI 简历解析引擎 (重点调研)**
  - [调研项] 寻找类似大模型API 进行结构化抽取、或市面成熟的 OCR 组件解析 PDF 的关键字段。
  - 解析完成后，展示极具震撼感效果的：原件左侧预览 + 右侧字段自动回填 UI。

---

## 💅 冲刺 2：将新视觉规范 “玻璃拟物化” 铺满老页面

虽然我们刚做好了 `JobListView.vue` 的样式升级，但现存的其他页面（比如 Dashboard 工作台和登录页）由于还在使用旧版本的样式，显得脱节。可以“一波带走”去感受强烈的视觉冲击：

- [ ] **TODO 4: 重构 `DashboardView.vue` 工作台**
  - 使用 `<el-row>` 和 `<el-col>` 全新布局。
  - 将数据块（如“本月入职”、“今日面试”）套用 `class="glass-panel hover-lift"`。
  - 让重点统计数字使用 `class="text-gradient"` 实现青紫渐变炫彩发光效果。
- [ ] **TODO 5: 重构 `LoginView.vue` 登录页**
  - 改用深空极简风。用深蓝色微透的亚克力毛玻璃底座包围登录框，大幅度提升高级感，增强系统“开场门面”。

---

## 🏢 冲刺 3：人员组织架构补充（权限分配依据）

为了保证后续 HR 和用人经理间的数据不出乱子（只能查看自己及所属部门的简历与岗位），我们必须先把管理后台里的人事映射补齐。

- [ ] **TODO 6: 部门信息树（前端 + 后端）**
  - 提供一张基础台账表（部门表）。
  - 实现一个前端层级的树形控件（Tree），支持管理员对内部员工角色与部门进行增删改归属映射。

---

## 🏃 冲刺 4：看板 (Kanban) 的骨架搭建

数据入库后，招聘就进入了“流转跟踪”形态。这是最终呈现产品价值的重要视图。

- [ ] **TODO 7: Kanban 数据流与组件设计**
  - 梳理对应的 `ats_application_pipeline` 状态跃迁后端 API (`POST /api/pipeline/move`)。
  - 引入前端轻量级的拖拽交互库。
  - 实现界面：横向分泳道（初筛、面试、Offer...），让 HR 能用鼠标体验沉浸式无缝拖拽推进流程。

---

**💡 我该从这里怎么做？**
只要您想启动，您可以随时在对话里指定上述列表里的一项或多项（比如：“_开始执行 TODO 4_” 或者 “_一起把 TODO 1 和 TODO 2 做了_”），我就会马上进行端到端全栈开发！
